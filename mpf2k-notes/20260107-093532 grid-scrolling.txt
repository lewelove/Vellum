grid-scrolling

the scroll behavior is discreet

row height is the ssot

each scroll input simply moves grid along y axis by row height = top gap + album image size + text size

when drawer expands it takes some amount of pixels = N * row height, where N is calculated based on how much space tracklist should take.


created:   2026-01-07 at 09:56 UTC+0300
modified:  2026-01-07 at 12:35 UTC+0300

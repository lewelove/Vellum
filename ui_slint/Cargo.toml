[package]
name = "ui_slint"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
slint = "1.15.0"
toml = "0.9.11"
walkdir = "2.5.0"

[build-dependencies]
slint-build = "1.15.0"
